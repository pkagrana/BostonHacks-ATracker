<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pratik.bostonhacks" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Home"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DataCollect"
            android:label="@string/title_activity_data_collect" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".ChooseDrink" -->
        <!-- android:label="@string/title_activity_choose_drink" > -->
        <!-- </activity> -->
        <activity
            android:name=".DisplayNextCourseOfAction"
            android:label="@string/title_activity_display_next_course_of_action" >
        </activity>
        <activity
            android:name=".AllOkay"
            android:label="@string/title_activity_all_okay" >
        </activity>
    </application>

</manifest>
